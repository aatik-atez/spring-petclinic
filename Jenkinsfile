pipeline {
    agent any

    environment {
        SERVICE_NAME = 'spring-petclinic'
    }
    stages {
       stage('path') {
          
            steps {
                 
                echo "$PATH"
                }
            }
        }
    
}

